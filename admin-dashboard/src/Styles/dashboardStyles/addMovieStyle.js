import styled from 'styled-components';

const AddMovieContainer = styled.div`
  padding: 20px;
`;

const Form = styled.form`
  display: flex;
  flex-direction: column;
  gap: 10px;
`;

export { AddMovieContainer, Form}