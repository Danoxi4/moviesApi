import styled from 'styled-components';

const UpdateMovieContainer = styled.div`
  padding: 20px;
`;

const Form = styled.form`
  display: flex;
  flex-direction: column;
  gap: 10px;
`;

export { Form, UpdateMovieContainer }